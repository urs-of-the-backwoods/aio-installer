# aio Client Installer

To install *aio* on your computer, simply clone this repository, switch to the directory of your operating system and run the installer batch file. It will copy the executable towards a directory, which is located in your path, so that you can run `aio` from the command line.
